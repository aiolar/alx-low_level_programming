#include "main.h"

/**
 * _strlen - returns the length of string
 * @s: The pointer to string
 *
 * Return: On success 1.
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
		(
		)
	return (i);
}
